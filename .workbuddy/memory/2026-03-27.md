# 2026-03-27

## 播放器修复清单全部完成
- 修复清单 `播放器问题修复清单.md` 全部 4 项完成，文档已删除
- 第 3 项 "undefined action!" 闪烁：用 `public/video-page-early.js` 纯 JS + `chrome.scripting.registerContentScripts` 解决
- 第 4 项 页面过渡闪烁：被第 3 项方案一并解决（early override 直接替换页面）
- 更新 MEMORY.md 中面包屑条目为新的 early override 方案记录

## 播放列表侧栏继续修复
- 确认播放列表数据已经能获取到（控制台日志显示收到 9 条），问题转为侧栏展开布局失效
- `public/video-page-early.js`、`src/player/index.html`、`src/player/core/overlay.ts` 统一改为更稳的 flex 布局：侧栏使用 `width + min-width + flex-basis` 同步控制
- 去掉播放列表头部和列表容器的 `min-width:300px`，改为 `width:100%`，避免内部最小宽度把父容器布局顶坏
- `setPlaylistOpen()` 增加 computedStyle 调试日志，用于确认侧栏展开后的真实计算宽度

